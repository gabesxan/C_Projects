# Imagens do projeto

Use esta pasta para capturas de tela, diagramas e imagens utilizadas pelo
`README.md`, pelo `manual.md` ou pelos documentos em `docs/`.

Assets internos da aplicação permanecem em `src/frontend/src/assets` ou
`src/frontend/public`, pois fazem parte do build do frontend.
